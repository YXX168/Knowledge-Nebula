# Host directory mounted into the container as /knowledge.
# Relative paths are resolved from the directory containing compose.yaml.
KNOWLEDGE_DIR=./knowledge

# Browser address: http://127.0.0.1:<port>
KNOWLEDGE_NEBULA_PORT=8765

# File-change scan interval in milliseconds (minimum 500).
SCAN_INTERVAL_MS=1200
